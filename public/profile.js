//const sum = user.values(unweighted_gpa)

//const sum = Object.values(data).reduce((acc, current) => acc + current.values, 0);
//const average = sum / Object.values(data).length;
//console.log(average);